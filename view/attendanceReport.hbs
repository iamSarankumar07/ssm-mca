<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Attendance Report</title>
    <link rel="icon" href="https://your-favicon-url.png">
    <style>
        @import url("https://fonts.googleapis.com/css2?family=Poppins:wght@200;300;400;500;600;700&display=swap");

        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
            font-family: "Poppins", sans-serif;
        }

        body {
            font-family: 'Arial', sans-serif;
            background-color: #f8f9fa;
            margin: 0;
            padding: 0;
        }

        .container {
            width: 100%;
            padding: 20px;
            background-color: #fff;
            box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
            border-radius: 5px;
            overflow: auto;
        }

        h1 {
            text-align: center;
            color: #fff;
            background-color: #3d6ef5ff;
            border-radius: 5px;
            padding: 10px;
            font-size: 24px;
            margin: 0;
        }

        h2 {
            color: #3d6ef5ff;
            margin-top: 20px;
        }

        table {
            width: 100%;
            border-collapse: collapse;
            margin: 20px auto;
            font-size: 15px;
        }

        th,
        td {
            padding: 8px;
            text-align: center;
            border: 1px solid #ddd;
        }

        .td {
            text-align: center;
        }

        th {
            background-color: #3d6ef5ff;
            color: #fff;
        }

        tr:hover {
            background-color: #f1f1f1;
        }

        .edit-icon,
        .delete-icon {
            text-decoration: none;
            color: #3d6ef5ff;
            transition: color 0.3s;
        }

        .edit-icon:hover,
        .delete-icon:hover {
            color: #0056b3;
        }

        .btn-container {
            text-align: center;
            margin-top: 20px;
        }

        .btn {
            padding: 8px 14px;
            color: #fff;
            border: 2px solid #3d6ef5ff;
            background: #3d6ef5ff;
            font-weight: bold;
            text-decoration: none;
            font-size: 12px;
            border-radius: 5px;
            cursor: pointer;
            transition: background 0.4s, color 0.4s;
        }

        .btn:hover {
            background: #fff;
            color: #3d6ef5ff;
        }

        .back-button {
            position: absolute;
            top: 3.5%;
            left: 2%;
            text-decoration: none;
        }

        .back-button img {
            width: 45px;
            height: 45px;
            margin-right: 16px;
            margin-bottom: 30px;
            vertical-align: middle;
            transition: width 0.3s ease, height 0.3s ease;
        }

        .icon-img {
            width: 25px;
            height: 25px;
            vertical-align: middle;
        }

        .view-link {
            text-decoration: none;
            cursor: pointer;
        }

        .view-link:hover .icon-img {
            transform: scale(1.2);
            filter: brightness(1.5);
        }

        .responsive-search-bar {
            padding: 6px;
            width: 100%;
            max-width: 300px;
            font-size: 14px;
            border: 1px solid #ccc;
            border-radius: 4px;
            box-sizing: border-box;
        }

        .attendance-radio {
            display: flex;
            justify-content: center;
            gap: 10px;
        }

        .attendance-radio label {
            margin: 0 5px;
        }

        input[type="radio"] {
            cursor: pointer;
        }

        input[type="date"] {
            padding: 6px;
            border: 1px solid #ccc;
            border-radius: 4px;
            font-size: 14px;
        }

        .filter-section {
            display: flex;
            flex-wrap: wrap;
            gap: 15px;
            justify-content: center;
            align-items: center;
            margin-bottom: 15px;
        }

        .filter-item {
            display: flex;
            flex-direction: column;
            align-items: center;
            text-align: center;
            width: auto;
            min-width: 200px;
            margin-top: 10px;
        }

        .filter-item input,
        .filter-item select {
            padding: 8px;
            font-size: 14px;
            border: 1px solid #ccc;
            border-radius: 4px;
            width: 100%;
            max-width: 200px;
        }

        .filter-button {
            display: flex;
            align-items: center;
            justify-content: center;
            width: 100%;
            margin-top: 10px;
        }

        @media (max-width: 768px) {
            h1 {
                font-size: 20px;
                padding: 10px;
            }

            h2 {
                font-size: 18px;
                margin: 10px;
            }

            table {
                font-size: 14px;
            }

            th,
            td {
                padding: 8px;
                font-size: 12px;
            }

            input[type="text"] {
                width: 100%;
                max-width: none;
            }

            .btn {
                padding: 12px 20px;
                font-size: 14px;
                width: auto;
                min-width: 150px;
                color: #fff;
                border: 2px solid #3d6ef5ff;
                background: #3d6ef5ff;
                font-weight: bold;
                text-decoration: none;
                border-radius: 5px;
                cursor: pointer;
                transition: background 0.4s, color 0.4s;
            }

            .btn:hover {
                background: #fff;
                color: #3d6ef5ff;
            }

            .icon-img {
                width: 20px;
                height: 20px;
            }

            .responsive-search-bar {
                max-width: 250px;
                font-size: 13px;
            }

            .back-button {
                position: absolute;
                top: 3.6%;
                left: 4%;
                text-decoration: none;
            }

            .back-button img {
                width: 40px;
                height: 40px;
                margin-right: 16px;
                margin-bottom: 30px;
                vertical-align: middle;
                transition: width 0.3s ease, height 0.3s ease;
            }

            .btn {
                font-size: 14px;
                padding: 10px 16px;
                min-width: 130px;
            }

            .filter-section {
                flex-direction: column;
                gap: 10px;
            }

            .filter-item {
                width: 100%;
                max-width: 300px;
            }

            .filter-item input,
            .filter-item select {
                max-width: 100%;
            }
        }

        @media (max-width: 480px) {
            table {
                display: block;
                overflow-x: auto;
                white-space: nowrap;
            }

            th,
            td {
                font-size: 10px;
                padding: 6px;
            }

            h1,
            h2 {
                font-size: 16px;
            }

            .responsive-search-bar {
                max-width: 200px;
                font-size: 12px;
            }

            .back-button {
                position: absolute;
                top: 3.5%;
                left: 7%;
                text-decoration: none;
            }

            .back-button img {
                width: 35px;
                height: 35px;
                margin-right: 16px;
                margin-bottom: 30px;
                vertical-align: middle;
                transition: width 0.3s ease, height 0.3s ease;
            }

            .btn {
                font-size: 12px;
                padding: 8px 14px;
                min-width: 120px;
            }

            .filter-section {
                flex-direction: column;
                gap: 8px;
            }

            .filter-item {
                width: 100%;
                max-width: 250px;
            }

            .filter-item input,
            .filter-item select {
                width: 100%;
            }
        }
    </style>
</head>

<body>
    <div class="container">
        <h1>MCA-{{year}} Attendance</h1>
        <a href="/ssm/mca/dashboard#attendance" class="back-button">
            <img src="https://i.postimg.cc/Ghw7c3bx/back-button-white.png" alt="Go Back">
        </a>

        <form action="/ssm/mca/getAttendenceReport" method="GET">
            <div class="filter-section">
                <div class="filter-item">
                    <label for="startDate"><strong>Start Date:</strong></label>
                    <input type="date" id="startDate" name="startDate" value="{{startDate}}" required>
                </div>

                <div class="filter-item">
                    <label for="endDate"><strong>End Date:</strong></label>
                    <input type="date" id="endDate" name="endDate" value="{{endDate}}" required>
                </div>

                {{!-- <div class="filter-item">
                    <label for="specificDate"><strong>Specific Date:</strong></label>
                    <input type="date" id="specificDate" name="specificDate" value="{{specificDate}}">
                </div> --}}

                <div class="filter-item">
                    <label for="year"><strong>Year:</strong></label>
                    <select name="year" id="year">
                        <option value="">All Years</option>
                        <option value="I" {{#if (eq year "I" )}}selected{{/if}}>MCA - I</option>
                        <option value="II" {{#if (eq year "II" )}}selected{{/if}}>MCA - II</option>
                    </select>
                </div>
            </div>

            <input type="hidden" name="isPdf" id="isPdf" value="false">

            <div class="filter-button" style="display: flex; gap: 10px; justify-content: center;">
                <button type="submit" class="btn" onclick="setPdf(false)">Get Report</button>
                <button type="submit" class="btn" onclick="setPdf(true)">Download</button>
            </div>

            {{!-- <div class="filter-button">
                <button type="submit" class="btn">Get Report</button>
            </div>
            <div class="filter-button">
                <button type="submit" class="btn">Download</button>
            </div> --}}
        </form>


        <table>
            <thead>
                <tr>
                    <th>S.No</th>
                    <th>Student Name</th>
                    <th>Student ID</th>
                    <th>Gender</th>
                    <th>Present</th>
                    <th>Absent</th>
                    <th>Late</th>
                    <th>Excused</th>
                </tr>
            </thead>
            <tbody>
                {{#each report}}
                <tr>
                    <td>{{inc @index}}</td>
                    <td style="text-align: left;">{{this.name}}</td>
                    <td>{{this.stdId}}</td>
                    <td>{{this.gender}}</td>
                    <td>{{this.totalPresent}}</td>
                    <td>{{this.totalAbsent}}</td>
                    <td>{{this.totalLate}}</td>
                    <td>{{this.totalExcused}}</td>
                </tr>
                {{/each}}
            </tbody>
        </table>

        <div class="btn-container">
            <a class="btn" href="/ssm/mca/dashboard#attendance">
                &lt; Go back!
            </a>
        </div>
    </div>
</body>

<script>
    function setPdf(value) {
        document.getElementById("isPdf").value = value ? "true" : "false";
    }
</script>

</html>