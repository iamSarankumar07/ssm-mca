<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Fee Information</title>
    <link rel="icon" href="https://your-favicon-url.png">
    <style>
        body {
            font-family: 'Arial', sans-serif;
            background-color: #f8f9fa;
            margin: 0;
            padding: 0;
            display: flex;
            justify-content: center;
            align-items: center;
            height: 100vh;
        }

        .container {
            width: 80%;
            max-width: 1200px;
            margin: 20px;
            padding: 20px;
            background-color: #fff;
            box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
            border-radius: 5px;
        }

        h1 {
            text-align: center;
            color: #fff;
            background-color: #007bff;
            border-radius: 5px;
            padding-top: 10px;
            padding-bottom: 10px;
            margin-top: 10px;
        }

        h2 {
            color: #007bff;
            margin-top: 20px;
        }

        table {
            width: 100%;
            border-collapse: collapse;
            margin-top: 20px;
        }

        th, td {
            border: 1px solid #dee2e6;
            text-align: center;
            padding: 12px;
        }

        th {
            background-color: #007bff;
            color: #fff;
        }

        .edit-icon, .delete-icon {
            text-decoration: none;
            color: #007bff;
            transition: color 0.3s;
        }

        .edit-icon:hover, .delete-icon:hover {
            color: #0056b3;
        }

        .btn-container {
            text-align: center;
            margin-top: 20px;
        }

        .btn {
            padding: 10px 20px;
            color: #fff;
            border: 2px solid #007bff;
            background: #007bff;
            font-weight: bold;
            text-decoration: none;
            font-size: 14px;
            border-radius: 5px;
            cursor: pointer;
            transition: background 0.4s, color 0.4s;
        }

        .btn:hover {
            background: #0056b3;
            color: #fff;
        }
        .goback {
    position: absolute;
    top: 10%;
    width: 45px;
    height: 45px;
    left: 10.5%;
}
        
    </style>
</head>
<body>
<div class="container">
    <h1>FEES DETAILS</h1>
    <h2>FIRST YEAR FEES DETAILS</h2>
    <table>
        <thead>
        <tr>
            <th>Name</th>
            <th>Total Fee</th>
            <th>Pending Fee</th>
            <th>Payment Status</th>
            <th>Edit</th>
        </tr>
        </thead>
        <tbody>
        {{#each firstYearStudents}}
        <tr>
            <td>{{name}}</td>
            <td>{{totalFee}}</td>
            <td>{{pendingFee}}</td>
            <td>{{paymentStatus}}</td>
            <td><a href="/ssm/mca/feeEdit/{{_id}}" class="edit-icon">Edit</a></td>
        </tr>
        {{/each}}
        </tbody>
    </table>
    <div class="btn-container">
        <button class="btn" onclick="printFirstYearFee()">Print First Year</button>
    </div>

    <h2>SECOND YEAR FEES DETAILS</h2>
    <table>
        <thead>
        <tr>
            <th>Name</th>
            <th>Total Fee</th>
            <th>Pending Fee</th>
            <th>Payment Status</th>
            <th>Edit</th>
        </tr>
        </thead>
        <tbody>
        {{#each secondYearStudents}}
        <tr>
            <td>{{name}}</td>
            <td>{{totalFee}}</td>
            <td>{{pendingFee}}</td>
            <td>{{paymentStatus}}</td>
            <td><a href="/ssm/mca/feeEdit/{{_id}}" class="edit-icon">Edit</a></td>
        </tr>
        {{/each}}
        </tbody>
    </table>
    <div class="btn-container">
        <button class="btn" onclick="printSecondYearFee()">Print Second Year</button>
    </div>
    <div class="btn-container">
        <a class="btn" href="/ssm/mca/dashboard">Go back!</a>
    </div>
</div>

<div id="printDiv" style="display:none;"></div>

<script>
    function printFirstYearFee() {
        var printWindow = window.open('', '_blank');
        
        var printContent = `
            <html>
            <head>
                <style>
                    body {
                        font-family: 'Arial', sans-serif;
                        background-color: #fff;
                        margin: 0; 
                        padding: 20px;
                    }

                    h1,h2,h3 {
                        color: #000; 
                        text-align: center;
                        margin-top: 20px;
                    }

                    table {
                        width: 100%; 
                        border-collapse: collapse;
                        margin-top: 20px;
                    }

                    th, td {
                        border: 1px solid #000; 
                        text-align: center;
                        padding: 12px;
                    }
                </style>
            </head>
            <body>
                <h1>SSM COLLEGE OF ENGINEERING</h1>
                <h2>Department Of MCA - I</h2>
                <h3>Tution Fees Details</h3>
                <table>
                    <thead>
                        <tr>
                            <th>Name</th>
                            <th>Total Fee</th>
                            <th>Pending Fee</th>
                            <th>Payment Status</th>
                        </tr>
                    </thead>
                    <tbody>
                        {{#each firstYearStudents}}
                        <tr>
                            <td>{{name}}</td>
                            <td>{{totalFee}}</td>
                            <td>{{pendingFee}}</td>
                            <td>{{paymentStatus}}</td>
                        </tr>
                        {{/each}}
                    </tbody>
                </table>
            </body>
            </html>
        `;
        
        printWindow.document.write(printContent);
        
        printWindow.document.close();
        printWindow.print();
    }

    function printSecondYearFee() {
        var printWindow = window.open('', '_blank');
        
        var printContent = `
            <html>
            <head>
                <style>
                    body {
                        font-family: 'Arial', sans-serif;
                        background-color: #fff; 
                        margin: 0; 
                        padding: 20px;
                    }

                    h1,h2,h3 {
                        color: #000;
                        text-align: center;
                        margin-top: 20px;
                    }

                    table {
                        width: 100%; 
                        border-collapse: collapse;
                        margin-top: 20px;
                    }

                    th, td {
                        border: 1px solid #000;
                        text-align: center;
                        padding: 12px;
                    }
                </style>
            </head>
            <body>
                <h1>SSM COLLEGE OF ENGINEERING</h1>
                <h2>Department Of MCA - II</h2>
                <h3>Tution Fees Details</h3>

                <table>
                    <thead>
                        <tr>
                            <th>Name</th>
                            <th>Total Fee</th>
                            <th>Pending Fee</th>
                            <th>Payment Status</th>
                        </tr>
                    </thead>
                    <tbody>
                        {{#each secondYearStudents}}
                        <tr>
                            <td>{{name}}</td>
                            <td>{{totalFee}}</td>
                            <td>{{pendingFee}}</td>
                            <td>{{paymentStatus}}</td>
                        </tr>
                        {{/each}}
                    </tbody>
                </table>
            </body>
            </html>
        `;
        
        printWindow.document.write(printContent);
        
        printWindow.document.close();
        printWindow.print();
    }
</script>


</body>
</html>
