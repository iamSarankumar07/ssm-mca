<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Unauthorized Access</title>
  <link rel="icon" href="https://i.postimg.cc/L5HvTvHg/expired.png" />
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
  <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@300;400;600&display=swap" rel="stylesheet">
  <style>
    :root {
      --primary: #4f46e5;
      --primary-hover: #4338ca;
      --secondary: #f9fafb;
    }

    body {
      font-family: 'Poppins', sans-serif;
      background: #e6ecf0;
      display: flex;
      justify-content: center;
      align-items: center;
      min-height: 100vh;
      margin: 0;
    }

    .unauth-container {
      background: #f0f3f7;
      border-radius: 20px;
      box-shadow: 12px 12px 30px #cdd3d8,
                  -12px -12px 30px #ffffff;
      text-align: center;
      padding: 50px 30px;
      width: 100%;
      max-width: 420px;
      animation: fadeIn 0.8s ease-in-out;
    }

    @keyframes fadeIn {
      from {
        opacity: 0;
        transform: translateY(20px);
      }
      to {
        opacity: 1;
        transform: translateY(0);
      }
    }

    .unauth-icon {
      width: 90px;
      margin-bottom: 25px;
      filter: drop-shadow(0 4px 8px rgba(0, 0, 0, 0.1));
    }

    .unauth-title {
      font-size: 26px;
      color: #2d2f38;
      font-weight: 600;
      margin-bottom: 10px;
    }

    .unauth-subtitle {
      font-size: 15px;
      color: #5f6368;
      margin-bottom: 30px;
      line-height: 1.6;
    }

    .btn-login {
      padding: 12px 30px;
      border-radius: 50px;
      font-weight: 500;
      font-size: 15px;
      transition: all 0.3s ease-in-out;
      background-color: var(--primary);
      border: none;
      color: var(--secondary);
    }

    .btn-login:hover {
      background-color: var(--primary-hover);
      color: var(--secondary);
      transform: translateY(-2px);
      box-shadow: 0 6px 20px rgba(0, 0, 0, 0.15);
    }

    @media (max-width: 480px) {
      .unauth-container {
        padding: 40px 20px;
      }
    }
  </style>
</head>

<body>
  <div class="unauth-container">
    <img src="https://i.postimg.cc/L5HvTvHg/expired.png" alt="Unauthorized" class="unauth-icon">
    <div class="unauth-title">Unauthorized Access</div>
    <div class="unauth-subtitle">
      Oops! You are not authorized to view this page.<br>
      Please login again to continue.
    </div>
    <a href="/v1/api/signin" class="btn btn-login">Login to continue</a>
  </div>
</body>

</html>
